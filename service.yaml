# mariadb
apiVersion: v1
kind: Service
metadata:
  name: mariadb-http
spec:
  ports:
  - port: 3306
  selector:
    app: mariadb
  clusterIP: None
---
# phpmyadmin
apiVersion: v1
kind: Service
metadata:
  labels:
    name: phpmyadmin
  name: phpmyadmin-http
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    name: phpmyadmin
  type: LoadBalancer
---
# jupyter
apiVersion: v1
kind: Service
metadata:
  name: jupyter-http
  labels:
    run: jupyter
  namespace: default
spec:
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: 8888
  selector:
    run: jupyter
  sessionAffinity: None
  type: LoadBalancer